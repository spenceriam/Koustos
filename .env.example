## Convex manages auth+secrets; only client-visible variables belong here.
## Use `npx convex env set <NAME> <value>` for server-only envs listed below.
# Required Convex env variables:
#   BETTER_AUTH_SECRET    # 32-byte base64 string for Better Auth session signing
#   SITE_URL              # e.g. http://localhost:3000 for local dev
#   RESEND_API_KEY        # transactional email key
#   OPENAI_API_KEY        # AI formatting model access
#   ENCRYPTION_KEY        # 32-byte hex string for PAT encryption
#   GOOGLE_CLIENT_ID      # Google OAuth client (dev + prod variants)
#   GOOGLE_CLIENT_SECRET  # Google OAuth secret
#   GITHUB_CLIENT_ID      # GitHub OAuth app client ID
#   GITHUB_CLIENT_SECRET  # GitHub OAuth app secret

# Populated automatically by `npx convex dev` for local development.
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CONVEX_SITE_URL=

# Public URL for the deployed Next.js app (configure in Vercel).
NEXT_PUBLIC_URL=https://koustos.dev
